import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  togglePasswordView: {
    fontSize: 12,
    fontWeight: 300,
    letterSpacing: 0.8,
  },
  button: {
    marginTop: 17,
  },
}));
