import makeStyles from '@material-ui/core/styles/makeStyles';

export default makeStyles(() => ({
  root: {
    marginTop: 8,
    color: '#959595',
    fontSize: '12px',
    letterSpacing: '.2px',
  },
}));
